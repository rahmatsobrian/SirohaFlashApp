org.gradle.jvmargs=-Xmx2560m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official
android.enableJetifier=false
# AGP 8.6.1 was only validated up to compileSdk 35; this project intentionally
# targets 36 (Android 16) per the "Android 10–16" requirement. The warning is
# informational only — suppressed so CI logs stay focused on real problems.
android.suppressUnsupportedCompileSdk=36
